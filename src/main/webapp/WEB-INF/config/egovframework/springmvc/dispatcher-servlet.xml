<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="
           http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd
           http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc.xsd">

    <!-- ========================================
         Spring MVC 설정 (DispatcherServlet Context)
         ======================================== -->

    <!-- Component Scan (Controller, RestController, WebMvcConfig) -->
    <context:component-scan base-package="kr.co.lms.web,kr.co.lms.config">
        <context:include-filter type="annotation" expression="org.springframework.stereotype.Controller"/>
        <context:include-filter type="annotation" expression="org.springframework.web.bind.annotation.RestController"/>
        <!-- WebMvcConfig는 Dispatcher Servlet Context에 필요 -->
        <context:include-filter type="annotation" expression="org.springframework.context.annotation.Configuration"/>
    </context:component-scan>

    <!-- MVC 기본 설정 활성화 -->
    <mvc:annotation-driven>
        <mvc:message-converters>
            <!-- JSON 변환기 (JSR310 모듈 등록된 ObjectMapper 사용) -->
            <bean class="org.springframework.http.converter.json.MappingJackson2HttpMessageConverter">
                <property name="objectMapper" ref="objectMapper"/>
            </bean>
        </mvc:message-converters>
    </mvc:annotation-driven>

    <!-- ========================================
         ViewResolver 설정 (JSP)
         ======================================== -->
    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/WEB-INF/jsp/"/>
        <property name="suffix" value=".jsp"/>
        <property name="order" value="1"/>
    </bean>

    <!-- ========================================
         정적 리소스 처리
         ======================================== -->
    <mvc:resources mapping="/css/**" location="/css/"/>
    <mvc:resources mapping="/js/**" location="/js/"/>
    <mvc:resources mapping="/images/**" location="/images/"/>
    <mvc:resources mapping="/react/**" location="/react/"/>

    <!-- ========================================
         CORS 설정
         ======================================== -->
    <mvc:cors>
        <mvc:mapping path="/api/**" 
                     allowed-origins="http://localhost:3000,http://localhost:3001"
                     allowed-methods="GET,POST,PUT,DELETE,PATCH,OPTIONS"
                     allowed-headers="*"
                     allow-credentials="true"
                     max-age="3600"/>
    </mvc:cors>

    <!-- ========================================
         멀티파트 리졸버 (파일 업로드)
         ======================================== -->
    <bean id="multipartResolver" class="org.springframework.web.multipart.commons.CommonsMultipartResolver">
        <property name="maxUploadSize" value="104857600"/>  <!-- 100MB -->
        <property name="maxInMemorySize" value="1048576"/>   <!-- 1MB -->
        <property name="defaultEncoding" value="UTF-8"/>
    </bean>

</beans>
