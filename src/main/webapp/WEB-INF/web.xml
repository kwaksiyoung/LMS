<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="http://xmlns.jcp.org/xml/ns/javaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/javaee 
         http://xmlns.jcp.org/xml/ns/javaee/web-app_4_0.xsd"
         version="4.0">

    <display-name>LMS</display-name>
    <description>Learning Management System (eGovFrame 4.2.0)</description>

    <!-- ========================================
         1. Spring Session Filter (개발 환경: 비활성화)
         Redis 서버가 없으므로 주석 처리
         프로덕션: Redis 구성 후 활성화
         ======================================== -->
    <!-- [주석 처리됨] Spring Session Filter
    <filter>
        <filter-name>springSessionRepositoryFilter</filter-name>
        <filter-class>org.springframework.web.filter.DelegatingFilterProxy</filter-class>
    </filter>
    <filter-mapping>
        <filter-name>springSessionRepositoryFilter</filter-name>
        <url-pattern>/*</url-pattern>
        <dispatcher>REQUEST</dispatcher>
        <dispatcher>ERROR</dispatcher>
    </filter-mapping>
    -->

    <!-- ========================================
         2. XSS 필터 (빈 정의 없음 - 주석 처리)
         ======================================== -->
    <!-- [주석 처리됨] XSS Filter Bean 정의 필요
    <filter>
        <filter-name>xssFilter</filter-name>
        <filter-class>org.springframework.web.filter.DelegatingFilterProxy</filter-class>
    </filter>
    <filter-mapping>
        <filter-name>xssFilter</filter-name>
        <url-pattern>/*</url-pattern>
    </filter-mapping>
    -->

    <!-- ========================================
         3. Character Encoding Filter
         ======================================== -->
    <filter>
        <filter-name>encodingFilter</filter-name>
        <filter-class>org.springframework.web.filter.CharacterEncodingFilter</filter-class>
        <init-param>
            <param-name>encoding</param-name>
            <param-value>UTF-8</param-value>
        </init-param>
        <init-param>
            <param-name>forceEncoding</param-name>
            <param-value>true</param-value>
        </init-param>
    </filter>
    <filter-mapping>
        <filter-name>encodingFilter</filter-name>
        <url-pattern>/*</url-pattern>
    </filter-mapping>

    <!-- ========================================
         4. JWT 인증 필터
         encodingFilter 다음에 위치 (UTF-8 인코딩 후 처리)
         /api/* 경로에만 적용
         ======================================== -->
    <filter>
        <filter-name>jwtAuthenticationFilter</filter-name>
        <filter-class>kr.co.lms.web.api.common.JwtAuthenticationFilter</filter-class>
    </filter>
    <filter-mapping>
        <filter-name>jwtAuthenticationFilter</filter-name>
        <url-pattern>/api/*</url-pattern>
    </filter-mapping>

    <!-- ========================================
         5. Spring Root Context 설정
         ======================================== -->
    <context-param>
        <param-name>contextConfigLocation</param-name>
        <param-value>
            classpath:egovframework/spring/context-common.xml,
            classpath:egovframework/spring/context-datasource.xml,
            classpath:egovframework/spring/context-session.xml
        </param-value>
    </context-param>

    <!-- Spring Context Loader Listener -->
    <listener>
        <listener-class>org.springframework.web.context.ContextLoaderListener</listener-class>
    </listener>

    <!-- ========================================
         6. Spring MVC Dispatcher Servlet
         ======================================== -->
    <servlet>
        <servlet-name>dispatcher</servlet-name>
        <servlet-class>org.springframework.web.servlet.DispatcherServlet</servlet-class>
        <init-param>
            <param-name>contextConfigLocation</param-name>
            <param-value>
                /WEB-INF/config/egovframework/springmvc/dispatcher-servlet.xml
            </param-value>
        </init-param>
        <load-on-startup>1</load-on-startup>
    </servlet>
    <servlet-mapping>
        <servlet-name>dispatcher</servlet-name>
        <url-pattern>/</url-pattern>
    </servlet-mapping>

    <!-- ========================================
         6. Session 설정
         ======================================== -->
    <session-config>
        <tracking-mode>COOKIE</tracking-mode>
        <cookie-config>
            <http-only>true</http-only>
            <secure>false</secure> <!-- HTTPS 환경에서는 true -->
            <max-age>1800</max-age>
        </cookie-config>
        <timeout-minutes>30</timeout-minutes>
    </session-config>

     <!-- ========================================
          7. Welcome File
          ======================================== -->
     <welcome-file-list>
         <welcome-file>index.jsp</welcome-file>
     </welcome-file-list>

     <!-- ========================================
          8. Error Pages
          ======================================== -->
     <error-page>
         <error-code>404</error-code>
         <location>/WEB-INF/jsp/error/404.jsp</location>
     </error-page>
     <error-page>
         <error-code>500</error-code>
         <location>/WEB-INF/jsp/error/500.jsp</location>
     </error-page>

    <!-- ========================================
         9. MIME Types
         ======================================== -->
    <mime-mapping>
        <extension>json</extension>
        <mime-type>application/json</mime-type>
    </mime-mapping>

</web-app>
